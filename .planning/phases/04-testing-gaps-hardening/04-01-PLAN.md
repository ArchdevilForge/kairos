---
phase: 04-testing-gaps-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_config_handler.py
  - src/pwatch/core/config_handler.py
autonomous: true
requirements: [R12]
must_haves:
  truths:
    - "ConfigHandler handles config updates correctly (apply, refresh, reload)"
    - "Auto mode fallback to default symbols when fetch_top_volume_symbols returns empty"
    - "Detector re-registration is tested after exchange reload"
  artifacts:
    - path: "tests/test_config_handler.py"
      provides: "Comprehensive ConfigHandler unit tests"
      min_lines: 200
    - path: "src/pwatch/core/config_handler.py"
      provides: "R12: graceful fallback for empty auto-mode symbols"
      contains: "get_default_symbols"
  key_links:
    - from: "tests/test_config_handler.py"
      to: "src/pwatch/core/config_handler.py"
      via: "import ConfigHandler"
      pattern: "from pwatch.core.config_handler import ConfigHandler"
---

<objective>
Write comprehensive unit tests for ConfigHandler (newly extracted in Phase 3) and implement R12 graceful degradation for empty symbol sets in auto mode.

Purpose: ConfigHandler has 0 dedicated tests and 70% coverage. R12 is the only unimplemented requirement — auto mode should fallback to default symbols when volume fetch fails.
Output: tests/test_config_handler.py (200+ lines), config_handler.py patched for R12
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/pwatch/core/config_handler.py
@src/pwatch/core/sentry.py
@src/pwatch/utils/default_symbols.py
@tests/conftest.py

From Phase 3 SUMMARY: ConfigHandler extracted from sentry.py — handles config updates, symbol sync, exchange reload, auto-refresh. Uses lazy `_sentry_module()` for test mock compatibility.

From Phase 2 SUMMARY: `_register_detectors_on()` extracts detector setup — clears `_detectors` before registering to prevent duplicates. `_exchange_lock` protects exchange reference during hot swap.

<interfaces>
From src/pwatch/core/config_handler.py:
```python
class ConfigHandler:
    AUTO_REFRESH_INTERVAL = 4 * 60 * 60
    def __init__(self, sentry) -> None
    def enqueue_config_update(self, event: "ConfigUpdateEvent") -> None
    def process_config_updates(self) -> None
    def apply_config_update(self, event: "ConfigUpdateEvent") -> None
    def refresh_runtime_settings(self) -> None
    def reload_runtime_components(self, event: "ConfigUpdateEvent") -> None
    def rebuild_notification_filter_locked(self) -> None
    def sync_symbols(self, exchange_name: str) -> None
    def sync_symbols_for_exchange(self, exchange, exchange_name: str) -> List[str]
    def check_auto_refresh(self) -> None
    def auto_mode_filters(self) -> dict[str, Any]
```

From src/pwatch/core/config_manager.py:
```python
@dataclass(frozen=True)
class ConfigDiff:
    changed_keys: set[str]
    requires_symbol_reload: bool

@dataclass(frozen=True)
class ConfigUpdateEvent:
    old_config: dict
    new_config: dict
    diff: ConfigDiff
    warnings: List[str]
```

From src/pwatch/utils/default_symbols.py:
```python
def get_default_symbols(exchange: str) -> list[str]  # Returns DEFAULT_TOP50_SYMBOLS copy
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ConfigHandler test scaffold + core config update tests</name>
  <files>tests/test_config_handler.py</files>
  <behavior>
    - Given a mock sentry with valid config, process_config_updates drains the queue and applies each event
    - Given a ConfigUpdateEvent with requires_symbol_reload=True, apply_config_update calls reload_runtime_components
    - Given a ConfigUpdateEvent with requires_symbol_reload=False, apply_config_update does NOT call reload_runtime_components
    - Given a config event with warnings, apply_config_update logs each warning
    - Given config update that exceeds 5s processing time, a warning is logged about exceeding target
    - refresh_runtime_settings parses defaultTimeframe and sets sentry.minutes
    - refresh_runtime_settings parses checkInterval and sets sentry._check_interval
    - refresh_runtime_settings pre-parses notificationCooldown and sets sentry._cooldown_seconds (R10)
    - refresh_runtime_settings handles invalid timeframe gracefully (keeps previous value)
    - rebuild_notification_filter_locked with "auto" selection clears notification_symbols
    - rebuild_notification_filter_locked with list of symbols sets notification_symbols to allowed subset
  </behavior>
  <action>
    Create tests/test_config_handler.py with:

    1. Helper function `_make_sentry(config)` that returns a MagicMock-based mock sentry with:
       - `_config_events = Queue()`
       - `_config_lock = RLock()`
       - `_exchange_lock = RLock()`
       - `config = config`
       - `matched_symbols = ["BTC/USDT:USDT"]`
       - `notification_symbols = None`
       - `_notification_symbol_set = set()`
       - `minutes = 5`, `threshold = 2.0`, `_check_interval = 300`, `_cooldown_seconds = 300`
       - `_auto_mode = False`, `_last_auto_refresh = 0`
       - `notifier = MagicMock()` with `update_config` method
       - `_velocity_detector = MagicMock()` with `update_config` method
       - `_volume_detector = MagicMock()` with `update_config` method
       - `_rebuild_notification_filter_locked = MagicMock()`
       - `_register_detectors_on = MagicMock()`

    2. Test class `TestConfigHandlerConfigUpdates`:
       - `test_process_config_updates_drains_queue`: Fill queue with 3 events, call process_config_updates, verify all applied
       - `test_process_config_updates_empty_queue`: Call with empty queue, returns immediately (no hang)
       - `test_apply_config_update_with_symbol_reload`: Create event with `requires_symbol_reload=True`, verify `reload_runtime_components` called
       - `test_apply_config_update_without_symbol_reload`: Create event with `requires_symbol_reload=False`, verify `reload_runtime_components` NOT called
       - `test_apply_config_update_with_warnings`: Event with warnings list, verify each warning logged
       - `test_apply_config_update_processing_time_warning`: Mock time to simulate >5s processing, verify warning logged

    3. Test class `TestConfigHandlerRuntimeSettings`:
       - `test_refresh_runtime_settings_parses_timeframe`: Config with "15m" timeframe → sentry.minutes = 15
       - `test_refresh_runtime_settings_parses_check_interval`: Config with checkInterval "1m" → sentry._check_interval = 60
       - `test_refresh_runtime_settings_pre_parses_cooldown`: Config with notificationCooldown "10m" → sentry._cooldown_seconds = 600
       - `test_refresh_runtime_settings_invalid_timeframe_keeps_previous`: Mock parse_timeframe to raise, verify sentry.minutes unchanged
       - `test_refresh_runtime_settings_invalid_cooldown_uses_default`: Mock parse_timeframe to raise for cooldown, verify _cooldown_seconds = 300.0
       - `test_refresh_runtime_settings_updates_notifier_and_detectors`: Verify notifier.update_config, _velocity_detector.update_config, _volume_detector.update_config all called with new config
       - `test_refresh_runtime_settings_updates_notification_filter`: Verify _rebuild_notification_filter_locked called

    4. Test class `TestConfigHandlerNotificationFilter`:
       - `test_rebuild_filter_auto_mode_clears_symbols`: config notificationSymbols="auto" → notification_symbols = None
       - `test_rebuild_filter_with_valid_symbols_list`: config has ["BTC/USDT:USDT"] in matched → notification_symbols = ["BTC/USDT:USDT"]
       - `test_rebuild_filter_rejects_symbols_not_in_monitored`: config has ["DOGE/USDT:USDT"] but monitored only BTC → notification_symbols = None, warning logged
       - `test_rebuild_filter_ignores_non_string_items`: config has [None, 123, "BTC/USDT:USDT"] → only valid strings considered

    Per TDD: Write tests FIRST (they should pass because ConfigHandler exists and is tested indirectly via test_core_sentry.py). If any test fails, fix the test mock setup — do NOT modify ConfigHandler unless a real bug is found.
  </action>
  <verify>
    <automated>uv run pytest tests/test_config_handler.py -v -x --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All 20+ ConfigHandler unit tests pass, covering config updates, runtime settings, notification filter, with no modifications to production code needed</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ConfigHandler exchange reload + auto-refresh tests + R12 implementation</name>
  <files>tests/test_config_handler.py, src/pwatch/core/config_handler.py</files>
  <behavior>
    - Given exchange reload succeeds, new exchange has detectors registered AND symbols synced
    - Given exchange reload fails during WebSocket start, old exchange is restored (rollback)
    - Given exchange reload fails during detector registration, new exchange is closed and old exchange kept
    - Given auto mode sync returns empty symbols AND no default symbols available, ValueError is raised
    - Given auto mode sync returns empty symbols, fallback to default symbols (R12)
    - Given check_auto_refresh when not in auto mode, returns immediately (no action)
    - Given check_auto_refresh within refresh interval, returns immediately
    - Given check_auto_refresh after interval with symbol changes, closes WS, re-registers detectors, swaps symbols, restarts WS
    - Given check_auto_refresh after interval with no symbol changes, only updates last_auto_refresh timestamp
  </behavior>
  <action>
    Add to tests/test_config_handler.py:

    5. Test class `TestConfigHandlerExchangeReload`:
       - `test_reload_runtime_components_success`: Mock sentry, mock get_exchange to return new exchange, mock WS start, mock detector registration → verify full 6-step reload completes, detectors registered on new exchange
       - `test_reload_runtime_components_rollback_on_ws_failure`: Mock WS start to raise Exception → verify old exchange restored under _exchange_lock
       - `test_reload_runtime_components_rollback_on_detector_failure`: Mock _register_detectors_on to raise → verify new exchange closed, old exchange kept

    6. Test class `TestConfigHandlerAutoRefresh`:
       - `test_check_auto_refresh_skips_when_not_auto_mode`: Set _auto_mode=False → no fetch_top_volume_symbols call
       - `test_check_auto_refresh_skips_within_interval`: Set _last_auto_refresh to current time → no refresh
       - `test_check_auto_refresh_triggers_after_interval_with_changes`: Mock fetch_top_volume_symbols returning different symbols → verify WS close, detector re-register, symbol swap, WS restart
       - `test_check_auto_refresh_no_changes_when_symbols_identical`: Mock fetch returning same symbols → only _last_auto_refresh updated
       - `test_check_auto_refresh_handles_empty_result_gracefully`: Mock fetch returning [] → keeps current symbols, no WS restart
       - `test_check_auto_refresh_handles_ws_restart_failure`: Mock WS start to raise during refresh → error logged, continues

    7. Test class `TestConfigHandlerSymbolSync`:
       - `test_sync_symbols_auto_mode_with_valid_symbols`: Mock fetch_top_volume_symbols → matched_symbols set, _auto_mode=True
       - `test_sync_symbols_manual_mode_with_valid_symbols`: Mock load_usdt_contracts, config with symbol list → matched_symbols filtered to valid ones
       - `test_sync_symbols_manual_mode_all_invalid_raises`: Config symbols none match available → ValueError raised with helpful message

    8. R12 Implementation — Modify `sync_symbols` in config_handler.py (lines 320-337):
       When `fetch_top_volume_symbols` returns empty in auto mode, instead of raising ValueError:
       - Log warning about fetch failure
       - Call `get_default_symbols(exchange_name)` as fallback
       - If default symbols also empty, then raise ValueError
       - Import `get_default_symbols` from `pwatch.utils.default_symbols` at top of file
       
       Modify `sync_symbols_for_exchange` similarly (lines 418-427):
       When fetch returns empty in auto mode, fall back to `get_default_symbols(exchange_name)`.
       If both fail, raise ValueError.

       This implements R12: "Fall back to last known good symbol set or manual mode symbols before failing."
       Using DEFAULT_TOP50_SYMBOLS as the fallback — these are always available and represent the most liquid contracts.
  </action>
  <verify>
    <automated>uv run pytest tests/test_config_handler.py -v -x --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>R12 implemented: auto-mode symbol fetch failure falls back to default symbols. All 35+ ConfigHandler tests pass. Exchange reload tests verify detector re-registration (R6/R7 coverage).</done>
</task>

<task type="auto">
  <name>Task 3: Config listener error handling tests (R4 verification)</name>
  <files>tests/test_config_handler.py</files>
  <behavior>
    - Given config_manager notifies listeners and one listener raises, the exception is logged (not swallowed)
    - Given config_manager notifies listeners with multiple listeners and one fails, other listeners still receive the event
    - Given _enqueue_config_update with a full queue (edge case), fallback to blocking put works
  </behavior>
  <action>
    Add to tests/test_config_handler.py:

    9. Test class `TestConfigHandlerErrorHandling`:
       - `test_enqueue_config_update_fallback_to_blocking_put`: Mock put_nowait to raise Full exception → verify put() called instead (this tests the except branch in _enqueue_config_update, lines 148-152 of sentry.py)
       - `test_process_config_updates_handles_malformed_event`: Put a non-ConfigUpdateEvent object in queue → verify error handled gracefully (doesn't crash loop)
       - `test_reload_runtime_components_handles_new_exchange_creation_failure`: Mock get_exchange to raise → verify error_handler called, function returns early without modifying sentry state

    10. Integration verification — write a test in tests/test_config_manager.py (append):
       - `test_config_manager_listener_exception_logged`: Create real ConfigManager, subscribe a listener that raises, trigger config change → verify logging.exception was called (tests R4: config listener errors are logged, not swallowed)
  </action>
  <verify>
    <automated>uv run pytest tests/test_config_handler.py tests/test_config_manager.py -v -x --tb=short -k "error or fallback or listener" 2>&1 | tail -20</automated>
  </verify>
  <done>Error handling tests pass, verifying R4 (config listener errors logged) and edge cases in config event processing</done>
</task>

</tasks>

<verification>
- All tests in tests/test_config_handler.py pass
- Coverage of config_handler.py ≥90% (was 70%)
- R12 implemented: auto mode falls back to default symbols on fetch failure
- No regression in existing tests: `uv run pytest --no-header -q 2>&1 | tail -5`
</verification>

<success_criteria>
- tests/test_config_handler.py exists with 35+ tests covering: config updates, runtime settings, notification filter, exchange reload, auto-refresh, symbol sync, error handling
- config_handler.py coverage ≥90%
- R12 implemented: empty auto-mode symbols → fallback to DEFAULT_TOP50_SYMBOLS
- All existing 502+ tests still pass
- No lint issues on changed files
</success_criteria>

<output>
After completion, create `.planning/phases/04-testing-gaps-hardening/04-01-SUMMARY.md`
</output>
