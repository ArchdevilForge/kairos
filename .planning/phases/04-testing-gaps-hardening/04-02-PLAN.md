---
phase: 04-testing-gaps-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_main_loop.py
  - tests/test_core_sentry.py
  - src/pwatch/core/sentry.py
autonomous: true
requirements: [R11]
must_haves:
  truths:
    - "MainLoopHandler starts WebSocket and enters monitoring loop"
    - "Anomaly events from WS detectors are processed and alerts sent"
    - "WebSocket health check detects disconnection and attempts reconnect"
    - "Empty symbol set in main loop skips price check gracefully"
  artifacts:
    - path: "tests/test_main_loop.py"
      provides: "MainLoopHandler async unit tests"
      min_lines: 150
    - path: "tests/test_core_sentry.py"
      provides: "Additional sentry edge case tests"
      min_lines: 50
  key_links:
    - from: "tests/test_main_loop.py"
      to: "src/pwatch/core/main_loop.py"
      via: "import MainLoopHandler"
      pattern: "from pwatch.core.main_loop import MainLoopHandler"
    - from: "src/pwatch/core/main_loop.py"
      to: "src/pwatch/core/sentry.py"
      via: "_sentry_module() lazy import"
      pattern: "import_module.*pwatch.core.sentry"
---

<objective>
Write comprehensive async unit tests for MainLoopHandler (extracted in Phase 3) and fill remaining sentry.py coverage gaps.

Purpose: MainLoopHandler has 0 dedicated tests and 76% coverage. R11 (asyncio.Event-based anomaly processing) needs verification.
Output: tests/test_main_loop.py (150+ lines), expanded tests/test_core_sentry.py
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/pwatch/core/main_loop.py
@src/pwatch/core/sentry.py
@src/pwatch/detectors/__init__.py
@tests/conftest.py

From Phase 3: MainLoopHandler extracted — uses asyncio.Event (R11) for immediate anomaly processing instead of 1s polling. Accesses monitor_top_movers and notification_cooldown via lazy _sentry_module() import.

From Phase 2: _exchange_lock protects exchange reference, _register_detectors_on clears before registering.

<interfaces>
From src/pwatch/core/main_loop.py:
```python
class MainLoopHandler:
    def __init__(self, sentry: Any) -> None
    async def run(self) -> None
    async def _check_price_movers(self, minutes, symbols, threshold, notification_filter) -> None
    async def _check_websocket_health(self) -> None
    async def _process_anomaly_events(self) -> None
    def signal_anomaly_event(self) -> None
```

From src/pwatch/detectors/__init__.py:
```python
@dataclass(frozen=True)
class AnomalyEvent:
    symbol: str
    event_type: str  # "price_velocity", "volume_spike", "batch_move"
    severity: str  # "LOW", "MEDIUM", "HIGH"
    data: Dict[str, Any]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MainLoopHandler core loop + anomaly event processing tests</name>
  <files>tests/test_main_loop.py</files>
  <behavior>
    - Given valid sentry, run() starts WebSocket, waits warmup, enters loop
    - Given WebSocket start fails, run() raises after logging error
    - Given anomaly events in queue, _process_anomaly_events drains, combines per-symbol, sends alert
    - Given anomaly event during cooldown, _process_anomaly_events skips alert
    - Given volume spike without price confirmation, _process_anomaly_events skips alert
    - Given empty anomaly queue, _process_anomaly_events returns immediately
    - signal_anomaly_event sets the asyncio.Event for R11 immediate processing
  </behavior>
  <action>
    Create tests/test_main_loop.py with:

    Helper `_make_mock_sentry(config=None)` returning MagicMock with:
    - `exchange = MagicMock()` with `start_websocket`, `close`, `ws_connected=True`, `check_ws_connection`
    - `matched_symbols = ["BTC/USDT:USDT", "ETH/USDT:USDT"]`
    - `_anomaly_events = Queue()` (real queue, not mock)
    - `_config_events = Queue()`
    - `_config_lock = RLock()`
    - `_exchange_lock = RLock()`
    - `_auto_mode = False`
    - `_ws_consecutive_failures = 0`, `_ws_max_failures = 5`, `_ws_alert_sent = False`
    - `_process_config_updates = MagicMock()`
    - `_check_auto_refresh = MagicMock()`
    - `_snapshot_runtime_state` returns `(5, 2.0, 300, ["BTC/USDT:USDT"], None)`
    - `_group_batch_events = MagicMock(side_effect=lambda x: {e["symbol"]: {"test": e} for e in x})`
    - `_format_combined_alert = MagicMock(return_value="Test alert")`
    - `_send_alert = MagicMock(return_value={"success": True, "reason": "sent"})`
    - `notifier = MagicMock()` with `send` method
    - `config = config or {"defaultTimeframe": "5m", "defaultThreshold": 2.0}`

    Test class `TestMainLoopHandlerInit`:
    - `test_init_creates_anomaly_event`: Verify _anomaly_event is asyncio.Event instance
    - `test_signal_anomaly_event_sets_flag`: Call signal_anomaly_event, verify _anomaly_event.is_set()

    Test class `TestMainLoopHandlerRun`:
    - `test_run_starts_websocket`: Mock asyncio.sleep to raise StopIteration after warmup → verify exchange.start_websocket called with matched_symbols
    - `test_run_raises_on_websocket_failure`: Mock start_websocket to raise → verify error_handler.handle_network_error called, exception re-raised
    - `test_run_processes_config_updates_in_loop`: Mock time.time to advance past check_interval, mock asyncio.sleep to raise after 2 iterations → verify _process_config_updates called
    - `test_run_skips_price_check_when_no_symbols`: Set matched_symbols=[], mock time to trigger check → verify warning logged, no monitor_top_movers call

    Test class `TestMainLoopHandlerAnomalyEvents`:
    - `test_process_anomaly_events_empty_queue`: Call with empty queue → returns immediately, no alert sent
    - `test_process_anomaly_events_sends_alert`: Put AnomalyEvent("BTC/USDT:USDT", "price_velocity", "HIGH", {}) → verify _send_alert called
    - `test_process_anomaly_events_skips_cooldown`: Mock notification_cooldown.should_notify to return False → verify no alert sent, info logged
    - `test_process_anomaly_events_volume_spike_without_price_confirmation`: Put AnomalyEvent with type "volume_spike" only → verify alert NOT sent (needs price confirmation)
    - `test_process_anomaly_events_volume_spike_with_price_confirmation`: Put BOTH volume_spike and price_velocity events → verify alert sent
    - `test_process_anomaly_events_deduplicates_by_type`: Put two events of same type for same symbol → only highest severity kept
    - `test_process_anomaly_events_handles_failed_send`: Mock _send_alert to return {"success": False, "reason": "rate limited"} → verify warning logged
  </action>
  <verify>
    <automated>uv run pytest tests/test_main_loop.py -v -x --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All 14+ MainLoopHandler tests pass, covering init, run loop, anomaly event processing with TDD</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MainLoopHandler WebSocket health + price mover tests</name>
  <files>tests/test_main_loop.py</files>
  <behavior>
    - Given WebSocket connected, _check_websocket_health resets failure counter
    - Given WebSocket disconnected, _check_websocket_health increments counter and attempts reconnect
    - Given 5+ consecutive WS failures, alert notification is sent
    - Given price movers detected, _check_price_movers sends batch alert
    - Given no price movers, _check_price_movers logs "no movements"
    - Given price mover check raises, error is logged and loop continues
  </behavior>
  <action>
    Add to tests/test_main_loop.py:

    Test class `TestMainLoopHandlerWebSocketHealth`:
    - `test_ws_health_connected_resets_counter`: Set ws_connected=True → verify _ws_consecutive_failures = 0
    - `test_ws_health_disconnected_increments_counter`: Set ws_connected=False, mock check_ws_connection to succeed → verify counter incremented, then reconnected
    - `test_ws_health_reconnect_fails`: Set ws_connected=False, mock check_ws_connection to raise → verify error_handler.handle_network_error called
    - `test_ws_health_alert_after_max_failures`: Set _ws_consecutive_failures = 5, ws_connected=False → verify notifier.send called with Chinese alert message
    - `test_ws_health_alert_not_sent_twice`: Set _ws_alert_sent=True, _ws_consecutive_failures=5 → verify notifier.send NOT called (already sent)

    Test class `TestMainLoopHandlerPriceMovers`:
    - `test_check_price_movers_no_movements`: Mock monitor_top_movers to return None → verify "No price movements" logged
    - `test_check_price_movers_with_movements`: Mock monitor_top_movers to return [{"symbol": "BTC/USDT:USDT", "change_pct": 5.0, ...}] → verify _group_batch_events, _format_combined_alert, _send_alert all called
    - `test_check_price_movers_alert_failure`: Mock _send_alert to return {"success": False, "reason": "timeout"} → verify warning logged
    - `test_check_price_movers_error_handling`: Mock monitor_top_movers to raise Exception → verify error_handler.handle_api_error called, no crash
  </action>
  <verify>
    <automated>uv run pytest tests/test_main_loop.py -v -x --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All 22+ MainLoopHandler tests pass, covering WebSocket health, price movers, and anomaly processing</done>
</task>

<task type="auto">
  <name>Task 3: Remaining sentry.py gaps + integration test (detector re-registration + full lifecycle)</name>
  <files>tests/test_core_sentry.py, tests/test_integration_lifecycle.py</files>
  <behavior>
    - Given detector registration, exchange._detectors.clear() is called before registering (no duplicates)
    - Given config update with anomaly event in queue, full flow: event → process → alert → cooldown
    - Given detector re-registration after exchange reload, new exchange has both detectors registered
  </behavior>
  <action>
    Append to tests/test_core_sentry.py:

    Test class `TestPriceSentryDetectorRegistration`:
    - `test_register_detectors_clears_before_registering`: Create PriceSentry with mock exchange that has pre-existing detectors → verify _detectors.clear() called before register_detector (prevents duplicate registration from Phase 2 fix)
    - `test_register_detectors_on_anomaly_callback`: Verify the _on_anomaly callback puts events in _anomaly_events queue AND calls _main_loop.signal_anomaly_event() (R11)
    - `test_anomaly_queue_overflow_fallback`: Mock _anomaly_events.put_nowait to raise, then verify fallback to blocking put is used (tests sentry.py lines 164-168 except branch)

    Create tests/test_integration_lifecycle.py:
    Integration test class `TestFullLifecycle`:
    - `test_full_start_alert_config_change_alert_cycle`: This is the key integration test.
      
      Setup: Create a mock PriceSentry with minimal config. Mock all external calls (exchange, notifier, fetch_top_volume_symbols).
      
      Steps:
      1. Initialize PriceSentry → verify matched_symbols set, detectors registered, WS started
      2. Simulate anomaly event → verify _process_anomaly_events processes it
      3. Simulate config update event (change threshold from 2.0 to 3.0) → verify _apply_config_update called, runtime settings refreshed
      4. Simulate another anomaly event → verify it processes with new threshold
      5. Verify full cycle completes without errors
      
      This tests the integration of: sentry → config_handler → main_loop → detectors → notifier

    - `test_detector_re_registration_after_exchange_reload`: 
      Setup: Create PriceSentry, manually call _reload_runtime_components (from ConfigHandler).
      Verify: New exchange instance has both _velocity_detector and _volume_detector registered (tests R6/R7 end-to-end).
  </action>
  <verify>
    <automated>uv run pytest tests/test_core_sentry.py tests/test_integration_lifecycle.py -v -x --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>Integration tests pass. Detector re-registration verified. Full lifecycle (start → alert → config change → alert) tested end-to-end.</done>
</task>

</tasks>

<verification>
- All tests in tests/test_main_loop.py pass (22+)
- All tests in tests/test_integration_lifecycle.py pass (2+)
- Additional sentry tests pass (3+)
- Coverage of main_loop.py ≥90% (was 76%)
- Coverage of sentry.py ≥90% (was 80%)
- Core modules overall coverage ≥85% (was 82%)
- No regression: `uv run pytest --no-header -q 2>&1 | tail -5`
</verification>

<success_criteria>
- tests/test_main_loop.py exists with 22+ async tests covering: init, run loop, anomaly processing, WebSocket health, price movers
- tests/test_integration_lifecycle.py exists with 2 integration tests: full lifecycle + detector re-registration
- main_loop.py coverage ≥90%, sentry.py coverage ≥90%
- Core modules overall coverage ≥85%
- All existing 502+ tests still pass
- No lint issues on changed files
</success_criteria>

<output>
After completion, create `.planning/phases/04-testing-gaps-hardening/04-02-SUMMARY.md`
</output>
