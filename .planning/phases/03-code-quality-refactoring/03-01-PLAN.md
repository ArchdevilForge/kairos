---
phase: 03-code-quality-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pwatch/paths.py
  - src/pwatch/utils/setup_logging.py
  - tests/test_app_runner.py
  - tests/test_cli_background_mode.py
  - tests/test_cli_misc.py
  - tests/test_cli_setup_wizard.py
  - tests/test_config_manager.py
  - tests/test_config_validator.py
  - tests/test_core_sentry.py
  - tests/test_default_symbols.py
  - tests/test_exchanges_base.py
  - tests/test_top_volume_symbols.py
  - tests/test_top_volume_symbols_gaps.py
autonomous: true
requirements:
  - R8

must_haves:
  truths:
    - "`uv run ruff check .` returns 0 errors"
    - "All 495 passing tests still pass"
  artifacts:
    - path: "src/pwatch/paths.py"
      provides: "Trailing newline at EOF"
    - path: "src/pwatch/utils/setup_logging.py"
      provides: "Trailing newline at EOF"
  key_links:
    - from: "tests/"
      to: "src/pwatch/"
      via: "import statements"
      pattern: "from pwatch"
---

<objective>
Fix all 14 ruff lint issues across the codebase. 13 are auto-fixable, 1 requires manual refactoring (E731 lambda → def).

Purpose: Clean baseline before structural refactoring — ensures any new lint issues introduced by extraction are attributable to the refactor, not pre-existing.
Output: Lint-clean codebase, all tests passing.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

Current lint output (14 issues):
```
src/pwatch/paths.py:37:43: W292 [*] No newline at end of file
src/pwatch/utils/setup_logging.py:32:57: W292 [*] No newline at end of file
tests/test_app_runner.py:1:1: I001 [*] Import block is un-sorted
tests/test_cli_background_mode.py:3:27: F401 [*] `unittest.mock.patch` imported but unused
tests/test_cli_background_mode.py:144:5: I001 [*] Import block is un-sorted
tests/test_cli_misc.py:3:38: F401 [*] `unittest.mock.patch` imported but unused
tests/test_cli_setup_wizard.py:103:55: W292 [*] No newline at end of file
tests/test_config_manager.py:103:5: E731 Do not assign a `lambda` expression, use a `def`
tests/test_config_validator.py:5:1: I001 [*] Import block is un-sorted
tests/test_core_sentry.py:5:1: I001 [*] Import block is un-sorted
tests/test_default_symbols.py:7:5: F401 [*] `PROMPTS` imported but unused
tests/test_exchanges_base.py:5:8: F401 [*] `asyncio` imported but unused
tests/test_top_volume_symbols.py:1:1: I001 [*] Import block is un-sorted
tests/test_top_volume_symbols_gaps.py:213:16: F401 [*] `time` imported but unused
```

13 fixable with `uv run ruff check . --fix`, 1 (E731) requires manual edit.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auto-fix 13 lint issues with ruff</name>
  <files>
    src/pwatch/paths.py
    src/pwatch/utils/setup_logging.py
    tests/test_app_runner.py
    tests/test_cli_background_mode.py
    tests/test_cli_misc.py
    tests/test_cli_setup_wizard.py
    tests/test_config_validator.py
    tests/test_core_sentry.py
    tests/test_default_symbols.py
    tests/test_exchanges_base.py
    tests/test_top_volume_symbols.py
    tests/test_top_volume_symbols_gaps.py
  </files>
  <action>
    Run `uv run ruff check . --fix` to auto-fix all 13 fixable issues:
    - W292 (3 files): Add trailing newline at EOF
    - I001 (5 files): Sort import blocks
    - F401 (5 files): Remove unused imports

    After running, verify the E731 issue at tests/test_config_manager.py:103 still exists (not auto-fixable).
  </action>
  <verify>
    <automated>uv run ruff check . 2>&1 | grep -v "E731" | grep -c "Found" && echo "13 issues fixed" || echo "check output"</automated>
  </verify>
  <done>13 of 14 lint issues resolved. Only E731 at tests/test_config_manager.py:103 remains.</done>
</task>

<task type="auto">
  <name>Task 2: Manually fix E731 lambda → def in test_config_manager.py</name>
  <files>tests/test_config_manager.py</files>
  <read_first>
    tests/test_config_manager.py (lines 95-115 to see lambda context)
  </read_first>
  <action>
    Read tests/test_config_manager.py around line 103. Find the lambda assignment (E731 violation).
    Convert `variable = lambda args: expression` to:
    ```python
    def variable(args):
        return expression
    ```
    Preserve the test's behavior exactly. Only change syntax, not logic.
  </action>
  <verify>
    <automated>uv run ruff check tests/test_config_manager.py --select E731</automated>
  </verify>
  <done>E731 resolved. `uv run ruff check .` returns "Found 0 errors."</done>
</task>

<task type="auto">
  <name>Task 3: Verify all tests pass after lint fixes</name>
  <files>tests/</files>
  <action>
    Run full test suite to confirm lint changes didn't break anything.
    Expected: 495 passed, 2 pre-existing failures in test_cli_misc.py (unrelated to lint changes).
  </action>
  <verify>
    <automated>uv run pytest --tb=short -q 2>&1 | tail -3</automated>
  </verify>
  <done>Test count unchanged: 495 passed, same 2 pre-existing failures. No regressions introduced.</done>
</task>

</tasks>

<verification>
- `uv run ruff check .` returns "Found 0 errors."
- `uv run pytest --tb=no -q` shows same pass/fail count as baseline (495 passed, 2 failed)
</verification>

<success_criteria>
- All 14 lint issues resolved (0 ruff errors)
- All 495 previously passing tests still pass
- No new test failures introduced
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-quality-refactoring/03-01-SUMMARY.md`
</output>
