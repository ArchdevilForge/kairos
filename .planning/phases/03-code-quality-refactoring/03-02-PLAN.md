---
phase: 03-code-quality-refactoring
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/pwatch/core/alert_formatter.py
  - src/pwatch/core/sentry.py
  - tests/test_core_sentry.py
autonomous: true
requirements:
  - R9

must_haves:
  truths:
    - "Alert formatting logic lives in its own module"
    - "sentry.py no longer contains _format_combined_alert or _group_batch_events"
    - "Existing tests pass without modification to test logic"
  artifacts:
    - path: "src/pwatch/core/alert_formatter.py"
      provides: "AlertFormatter class with format_combined_alert and group_batch_events"
      exports: ["AlertFormatter"]
    - path: "src/pwatch/core/sentry.py"
      provides: "PriceSentry delegates to AlertFormatter"
      max_lines: 850
  key_links:
    - from: "src/pwatch/core/sentry.py"
      to: "src/pwatch/core/alert_formatter.py"
      via: "import and delegation"
      pattern: "from pwatch.core.alert_formatter import AlertFormatter"
---

<objective>
Extract alert formatting logic from sentry.py into a dedicated `alert_formatter.py` module. This is the first step of splitting sentry.py (R9) — extracting the simplest, most self-contained responsibility.

Purpose: Reduce sentry.py complexity by extracting pure formatting logic that has no dependencies on sentry state (both methods are @staticmethod or simple grouping).
Output: New `alert_formatter.py` module, sentry.py delegates to it, tests updated.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/pwatch/core/sentry.py (lines 345-468: _group_batch_events, _format_combined_alert)
@tests/test_core_sentry.py

**Extract these two methods from sentry.py:**

1. `_group_batch_events` (lines 345-350): Groups batch events by symbol. Simple dict grouping.

2. `_format_combined_alert` (lines 408-468, @staticmethod): Formats batch and realtime facts into one alert message. Pure function — takes symbol + events dict, returns formatted string. No state access.

Both are pure/stateless functions — ideal first extraction.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/pwatch/core/alert_formatter.py</name>
  <files>src/pwatch/core/alert_formatter.py</files>
  <action>
    Create a new module `src/pwatch/core/alert_formatter.py` with an `AlertFormatter` class containing:

    1. `@staticmethod group_batch_events(events: list[dict]) -> dict[str, dict[str, dict]]`:
       - Exact copy of `_group_batch_events` from sentry.py (lines 345-350)
       - Groups events by symbol key

    2. `@staticmethod format_combined_alert(symbol: str, events: dict[str, Any]) -> str`:
       - Exact copy of `_format_combined_alert` from sentry.py (lines 408-468)
       - Formats batch and realtime facts into one alert string
       - No changes to logic — direct copy

    Add proper module docstring: "Alert formatting for PriceSentry notifications."
    Include `from __future__ import annotations` and necessary imports (`Any` from typing).
  </action>
  <verify>
    <automated>python -c "from pwatch.core.alert_formatter import AlertFormatter; print('OK')"</automated>
  </verify>
  <done>File exists with AlertFormatter class, both static methods, importable without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Update sentry.py to delegate to AlertFormatter</name>
  <files>src/pwatch/core/sentry.py</files>
  <read_first>
    src/pwatch/core/sentry.py (lines 1-22 for imports, lines 345-350, lines 408-468)
    src/pwatch/core/alert_formatter.py (just created)
  </read_first>
  <action>
    1. Add import: `from pwatch.core.alert_formatter import AlertFormatter` to sentry.py imports (alphabetically sorted per ruff I rule)

    2. Replace `_group_batch_events` method body with delegation:
       ```python
       def _group_batch_events(self, events: list[dict]) -> dict[str, dict[str, dict]]:
           return AlertFormatter.group_batch_events(events)
       ```

    3. Replace `_format_combined_alert` method body with delegation:
       ```python
       @staticmethod
       def _format_combined_alert(symbol: str, events: dict[str, Any]) -> str:
           return AlertFormatter.format_combined_alert(symbol, events)
       ```

    Keep the method signatures unchanged — tests may patch/call these methods directly.
    Do NOT remove the methods entirely (preserves backward compatibility for tests and detectors).
  </action>
  <verify>
    <automated>uv run ruff check src/pwatch/core/sentry.py</automated>
  </verify>
  <done>sentry.py imports AlertFormatter, delegates to it. Method signatures preserved. Lint clean.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for AlertFormatter and verify existing tests pass</name>
  <files>tests/test_core_sentry.py</files>
  <read_first>
    tests/test_core_sentry.py (find tests that use _format_combined_alert or _group_batch_events)
    src/pwatch/core/alert_formatter.py
  </read_first>
  <action>
    1. Run existing tests: `uv run pytest tests/test_core_sentry.py -v` — all must pass (delegation preserves behavior)

    2. Add a new test file `tests/test_alert_formatter.py` with:
       - `test_group_batch_events_empty`: empty list returns empty dict
       - `test_group_batch_events_single`: single event grouped correctly
       - `test_group_batch_events_multiple`: multiple events for same symbol grouped
       - `test_format_combined_alert_empty`: empty events returns empty string
       - `test_format_combined_alert_price_only`: price_velocity event formats correctly
       - `test_format_combined_alert_volume_only`: volume_spike without price confirmation (edge case)
       - `test_format_combined_alert_combined`: price + volume events combine

    3. Run full test suite to confirm no regressions
  </action>
  <verify>
    <automated>uv run pytest tests/test_alert_formatter.py tests/test_core_sentry.py -v --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>AlertFormatter has dedicated tests. All existing sentry tests pass. sentry.py line count reduced by ~85 lines.</done>
</task>

</tasks>

<verification>
- `src/pwatch/core/alert_formatter.py` exists with AlertFormatter class
- `src/pwatch/core/sentry.py` delegates to AlertFormatter, method signatures preserved
- `uv run ruff check src/pwatch/core/` returns 0 errors
- `uv run pytest tests/test_core_sentry.py tests/test_alert_formatter.py -v` all pass
- sentry.py line count < 850 (was 905, ~85 lines extracted)
</verification>

<success_criteria>
- alert_formatter.py created with both methods
- sentry.py delegates, signatures preserved for backward compat
- New tests for AlertFormatter added
- All existing tests pass
- sentry.py reduced below 850 lines
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-quality-refactoring/03-02-SUMMARY.md`
</output>
