[project]
name = "kairos-research"
version = "0.1.0"
description = "JSONL → DuckDB research layer: 复盘/归因查询层(raw JSONL 保持 source of truth)"
requires-python = ">=3.10"
dependencies = ["duckdb>=1.0"]

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.uv]
package = false
